{
  "name": "vertravels-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apps/web && pnpm install && pnpm run build",
    "dev": "cd apps/web && pnpm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
